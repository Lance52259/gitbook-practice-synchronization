## Summary

This PR is auto-generated by **gitbook-practice-synchronization** to sync a best-practice example from source repo **B** (`{b_repo}`) into **bilingual** (zh-cn + en-us) docs in target repo **C** (`{c_repo}`).

| Item | Value |
|------|-------|
| Service | `{service}` |
| Practice | `{simple_title}` |
| Source practice ID | `{practice_id}` |
| Source repo (B) | `{b_repo}@{b_sha}` |
| Target repo (C) | `{c_repo}` |
| Generator | Skill `{skill_id}` + `{ai_provider}` |

## Motivation

A new best practice appeared under B repo `examples/` that is not yet covered in C. The change adds **Chinese and English** practice bodies per C-repo / Skill conventions and updates navigation (`index.md` / `SUMMARY.md`, plus `README.md` when the service is new). English navigation is ordered first (alphabetical); Chinese follows the same path order.

## Changes

{ai_summary}

### Files

{file_list}

## Review checklist

### Document structure (Skill / template)

- [ ] Chinese body: `docs/zh-cn/best-practices/{service}/…`
- [ ] English body: `docs/en-us/best-practices/{service}/…` (same `{service}/{practice}` as Chinese)
- [ ] H1 style: ZH `部署…` / EN `Deploy …`; required sections present in both locales
- [ ] EN Reference source-link anchor: `Best Practice Source Code Reference For {ServiceName} {PracticeObject}` (service name + object; **no** `Deploy`)
- [ ] HCL matches source scripts; no real AK/SK/passwords (placeholders are OK)
- [ ] No duplicate `variable "name"` across operation steps

### Navigation and index (EN first, then ZH follows EN order)

- [ ] Updated EN `index.md` / `SUMMARY.md` (and README if new service)
- [ ] Updated ZH `index.md` / `SUMMARY.md` (and README if new service) aligned with EN order
- [ ] No destructive rewrite of either `SUMMARY.md`

### Before merge

- [ ] Title matches: `docs({service}): support new best practice for {simple_title}`
- [ ] Spot-check command blocks and dependency tree
- [ ] Merge when ready; after merge this practice is considered synced
- [ ] Note: while this PR is open, gitbook-practice-synchronization will not open another PR for the same `{service}` until the next scan after merge

---

*Generated by gitbook-practice-synchronization. Please review before merge.*
